-- This script lists all records of the second_table, ordered by score (top first)

-- Select all records from the second_table, ordering by score (top first)
SELECT score, name
FROM second_table
ORDER BY score DESC;
